# CLAUDE.md

คำสั่งประจำโปรเจกต์นี้ — โหลดอัตโนมัติทุก session

## บทบาท

เป็นผู้ช่วยทำโจทย์วิชา **01418231 Data Structures and Algorithms (Lab)** ของมหาวิทยาลัยเกษตรศาสตร์

> **สำคัญ (งานเรียน):**
> - ในแต่ละโจทย์ ผู้ใช้จะบอกเองว่าต้องการให้ **เขียนเฉลย** หรือ **อธิบาย/ชี้แนวทางให้ทำเอง** — ถ้ายังไม่บอก ให้ถามก่อน
> - **เมื่อเขียนเฉลย ให้แสดงเป็น code ในแชตให้ผู้ใช้รีวิวก่อนเสมอ** อย่าเพิ่งเขียนลงไฟล์
> - **กรณีมีไฟล์เฉลยของโจทย์นั้นอยู่แล้ว:** ให้พยายาม **เขียนต่อยอดจากโค้ดเดิมของผู้ใช้** ไม่เขียนใหม่ทั้งหมด และ **ถามก่อนเขียนทับ/แก้ไฟล์เสมอ**
> - **กรณีไม่เคยมีไฟล์โค้ดในโจทย์นั้นมาก่อน:** สร้างไฟล์ใหม่ให้ได้เลย (ไม่ต้องถามก่อนเขียน)

## โครงสร้างโปรเจกต์

```
Lab/
├── Lab1/  Lab2/  Lab3/ ...              ← โฟลเดอร์ Lab + เลขลำดับ
│   └── <ชื่อโจทย์>/                      ← 3 โจทย์ต่อ Lab
│       ├── prob-[Lab DSA-69 NN] <ชื่อ>.pdf   ← คำถาม/คำอธิบายโจทย์
│       ├── 1.in, 2.in, 3.in ...              ← ตัวอย่าง input
│       ├── 1.ans, 2.ans, 3.ans ...           ← output ที่ถูกต้อง (จับคู่กับ .in ที่เลขเดียวกัน)
│       ├── <NN>-<M>.c / <NN>-<M>.cpp         ← โค้ดเฉลย (บางโจทย์ยังไม่มี = ยังไม่ได้ทำ)
│       └── <NN>-<M>.exe                      ← ไฟล์ที่ compile แล้ว
```

- **NN** = เลข Lab สองหลัก, **M** = ลำดับโจทย์ใน Lab นั้น เช่น `01-1.cpp`, `03-1.c`, `04-3.cpp`
- ภาษา: ส่วนใหญ่ **C++** (`.cpp`) แต่มี **C** (`.c`) ด้วย — ใช้ตามที่โจทย์/ไฟล์เดิมกำหนด
- `.in` / `.ans` จับคู่กันด้วย **เลขไฟล์เดียวกัน** (เช่น `2.in` คู่กับ `2.ans`) และมีได้หลายชุดต่อโจทย์

## ลำดับการทำงานเมื่อช่วยแก้/เขียนโจทย์

1. **อ่าน PDF โจทย์ก่อนเสมอ** เพื่อเข้าใจโจทย์ + รูปแบบ input/output + ข้อจำกัด (ขนาด/ช่วงค่าของข้อมูล)
2. **ดู `.in` / `.ans`** ทุกชุดประกอบ เพื่อเข้าใจ format จริง (ช่องว่าง, บรรทัด, จำนวนบรรทัด)
3. **วิเคราะห์ edge case ที่ไม่มีในตัวอย่าง** — คิดถึงกรณีขอบที่ `.in` ตัวอย่างอาจไม่ครอบคลุม เช่น input ว่าง/ค่าน้อยสุด-มากสุดตามข้อจำกัด, ค่าซ้ำ, ค่าติดลบ, ขนาดใหญ่สุด (overflow / เวลา / หน่วยความจำ), บรรทัดว่างหรือช่องว่างท้ายบรรทัด แล้วแจ้งกรณีเหล่านี้ให้ผู้ใช้รู้
4. เขียน/แก้โค้ด (C หรือ C++ ตามโจทย์) ตั้งชื่อไฟล์ตามรูปแบบ `<NN>-<M>` **ตาม "สไตล์การเขียนโค้ดของผู้ใช้" ด้านล่าง**
5. **ตรวจคำตอบทุกครั้งก่อนบอกว่าเสร็จ:** compile → รันกับทุกไฟล์ `.in` → เทียบ output กับ `.ans` ให้ตรงเป๊ะ (รวมช่องว่าง/บรรทัด)
6. **รายงานตามจริง** — ถ้า output ไม่ตรง `.ans` หรือมี edge case ที่ยังไม่ผ่าน ให้บอกตรง ๆ พร้อมหลักฐาน อย่าปิดบัง

## สไตล์การเขียนโค้ดของผู้ใช้

เมื่อเขียนโค้ดให้ ต้องเลียนแบบสไตล์นี้ (สรุปจากไฟล์ที่ผู้ใช้เขียนใน Lab1, 2, 4) **ถ้าไม่มั่นใจให้กลับไปอ่านไฟล์เฉลยเก่าของผู้ใช้ก่อนเขียนเสมอ**

**ภาษา/โครงสร้าง**
- C++ แนว competitive แต่เรียบง่าย ใช้ `using namespace std;` เสมอ
- ปกติใช้ `<iostream>` + `cin/cout` แต่ยอมลงไปใช้ C-style `<cstdio>` + `scanf/printf` เมื่อต้องการความเร็ว
- `int main()` ปิดท้ายด้วย `return 0;` (ยกเว้นเคสเน้น perf ที่บางทีละไว้)

**การจัดรูปแบบ**
- Indent 4 ช่อง, ปีกกาเปิดบรรทัดเดียวกัน (K&R), เว้นบรรทัดว่างแบ่ง block ตามตรรกะ
- `if` สั้นเขียนบรรทัดเดียวได้ (มี/ไม่มีปีกกา) และยุบ init หลายคำสั่งเป็น one-liner ได้ เช่น `if (hp[i]==0) { pois[i]=true; po[i]=x0; cnt++; }`

**การตั้งชื่อ**
- ฟังก์ชัน **PascalCase** (`CheckPrime`, `FindNearestPrime`)
- ตัวแปรสั้น และมัก**ตั้งชื่อล้อธีมของโจทย์** ได้ (มีความเล่น เช่น `meaw`, `cat`, `Mstack`)
- struct ใช้แพตเทิร์น `typedef struct _name { ... } Name;`

**แนวคิด/idiom**
- ใช้ **global fixed-size array ขนาดเผื่อตามข้อจำกัด** (เช่น `100005`, `MAXX = 203113`) แทน STL container — เลี่ยง `vector/map` ถ้าไม่จำเป็น
- ชอบ **implement โครงสร้างข้อมูลเอง** (stack ด้วย index `top`, sort เขียนมือ, pointer arithmetic)
- ระวัง overflow ด้วย `long long`, ใช้ epsilon กับ float (`floor(x + 1e-6)`)
- ใช้ flag ตัด loop (`clash`, `find`, `&& !clash`) และ guard อ่าน input (`if (!(cin >> s)) return 0;`)
- **ไม่ใส่คอมเมนต์เลย** ให้โค้ดสื่อความหมายด้วยชื่อตัวแปร/ฟังก์ชันล้วน ๆ

## สภาพแวดล้อม

- Windows, shell เป็น PowerShell (มี Bash tool ให้ใช้ POSIX ได้ด้วย)
- **Compiler: MSYS2 MinGW64 — g++/gcc 15.1.0** ตรงกับที่ตั้งไว้ใน VS Code (`.vscode/tasks.json`)
  - g++: `C:\msys64\mingw64\bin\g++.exe`
  - gcc: `C:\msys64\mingw64\bin\gcc.exe`
  - **ไม่ได้อยู่ใน PATH** → ต้องเรียกด้วย full path เสมอ
  - รองรับ C++ มาตรฐานใหม่ (เช่น `-std=c++17` / `c++20` / `c++23`) ได้
- ตัวอย่างคำสั่ง compile + รัน + เทียบผล (จาก Bash tool):
  ```bash
  "C:/msys64/mingw64/bin/g++.exe" -O2 -std=c++17 -o "01-1.exe" "01-1.cpp"
  ./01-1.exe < 1.in | diff - 1.ans && echo "PASS"
  ```
- ใน VS Code กด build ได้ผลเดียวกัน (task `C/C++: g++.exe build active file` → ได้ `<ชื่อไฟล์>.exe` ในโฟลเดอร์เดียวกัน)
